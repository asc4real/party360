package com.lms.party360.app.command;

import com.lms.party360.api.model.response.CreatePartyResponse;
import io.spring.gradle.dependencymanagement.org.apache.maven.artifact.repository.Authentication;

import java.util.UUID;

public class CreateBusinessHandler {
    public CreatePartyResponse handle(UUID idem, CreateBusinessRequest request, Authentication auth) {
    }
}
